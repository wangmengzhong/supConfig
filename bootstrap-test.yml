spring:
  profiles:
    active: test
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/wangmengzhong/supConfig
          searchPaths: auth
        label: master
